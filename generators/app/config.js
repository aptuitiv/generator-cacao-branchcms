/**
 * Configuration for scaffolding the project.
 *
 * This provides a central spot to make configuration changes instead of
 * having to edit the index.js file when packages are updated.
 */

const config = {
    // Versions to use in the package.json when including
    // these packages
    packages: {
        'magnific-popup': '^1.1.0',
        'slick-carousel': '^1.8.1',
    },
};

export default config;
